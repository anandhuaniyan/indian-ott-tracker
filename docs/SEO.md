# SEO

The API exposes `robots.txt`, `sitemap.xml` and `ads.txt`. The sitemap streams movie and person URL rows from database queries without loading the catalogue into memory. The frontend establishes descriptive page metadata and semantic links; deploy it behind HTTPS with canonical `SITE_URL`. Add production JSON-LD and per-route server rendering when adopting a public SSR frontend host.
