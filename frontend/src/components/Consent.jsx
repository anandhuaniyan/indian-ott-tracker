import {useState} from "react";
export default function Consent(){const [open,setOpen]=useState(!localStorage.getItem("ott-consent"));if(!open)return null;const save=(analytics,advertising)=>{localStorage.setItem("ott-consent",JSON.stringify({necessary:true,analytics,advertising}));setOpen(false);location.reload()};return <aside role="dialog" aria-label="Cookie preferences"><strong>Your privacy choices</strong><p>Essential storage keeps this site working. Analytics and advertising are optional.</p><button onClick={()=>save(false,false)}>Essential only</button><button onClick={()=>save(true,true)}>Accept optional cookies</button></aside>}
